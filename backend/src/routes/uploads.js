const express = require('express');
const router = express.Router();

// Upload routes disabled - not used in this application
// If needed in the future, restore from git history

module.exports = router;
