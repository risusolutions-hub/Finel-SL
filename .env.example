# Environment variables for local development
# Set ALLOW_PUBLIC_COMPLAINTS to 'false' to require authentication for public endpoints like complaint creation and customer service lookup.
# Default behavior (when unset) is to allow public complaint creation to preserve backward compatibility in dev.

SESSION_SECRET=keyboard_cat123
MONGO_URI=mongodb://localhost:27017/sparkel
PORT=4000
